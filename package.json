{
  "name": "portfolio-website",
  "private": true,
  "description": "Repo-level workspace root. Only wires up the Next.js app (web/) and its backend package (backend/) as an npm workspace — frontend/ (the legacy Vite SPA, still deployed until cutover) is deliberately NOT a workspace member, so it keeps its own independent install/build lifecycle untouched.",
  "workspaces": [
    "web",
    "backend"
  ],
  "overrides": {
    "react": "19.2.7",
    "react-dom": "19.2.7"
  }
}
