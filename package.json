{
  "name": "portfolio-website",
  "private": true,
  "description": "Repo-level workspace root. Only wires up the Next.js app (web/) and its backend package (backend/) as an npm workspace — frontend/ (the legacy Vite SPA) has already been cut over from in production and is kept only pending its removal, so it's deliberately NOT a workspace member and keeps its own independent install/build lifecycle untouched until it's deleted.",
  "workspaces": [
    "web",
    "backend"
  ],
  "overrides": {
    "react": "19.2.7",
    "react-dom": "19.2.7"
  }
}
