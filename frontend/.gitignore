.env
.env.test
/node_modules/
/dist/
*.tsbuildinfo

# Playwright — generated on every run, never commit. tests/visual-snapshots/ is NOT listed here
# on purpose: those PNGs are the committed baseline "references" (see tests/README.md).
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/