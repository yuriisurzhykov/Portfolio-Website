import { defineContract } from "@portfolio/design-tokens";

/** Empty required list — see `contracts/radius.ts`'s identical comment; same "structure now, not yet consumed" status. */
export const spacingContract = defineContract({ category: "spacing", required: [] });
