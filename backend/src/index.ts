// Placeholder entry point — filled in starting Phase 2 (Prisma client, auth,
// services). Kept as a real, importable module now so the workspace wiring
// (npm workspaces + Next.js transpilePackages) can be verified end-to-end
// before Phase 2 lands real logic here.
export const BACKEND_PACKAGE_READY = true;
