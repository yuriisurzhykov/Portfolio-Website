# pages/work-detail — WorkDetailPage

Тот же паттерн и те же причины, что в `pages/journal-detail/README.md`
(`useParams` → `next/navigation`, `<Navigate/>` → `useRouter().replace()` в
эффекте, компонент остаётся Client Component из-за `useTranslation()`).
Не дублирую весь разбор здесь — см. соседний README.
